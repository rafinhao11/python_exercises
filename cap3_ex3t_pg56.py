#formula velocidade projetil
#velocidade = (distancia * 1000) / (tempo * 60)

distancia = float(input("Distancia: "))
tempo = float(input("Tempo: "))

velocidade = (distancia * 1000) / (tempo * 60)
print(velocidade)